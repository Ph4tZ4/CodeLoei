# CodeLoei (ระบบเก็บผลงานนักศึกษาไอที)

โปรเจกต์นี้เป็นเว็บแอปพลิเคชันสำหรับเก็บรวบรวมและแสดงผลงาน (Portfolio/Projects) ของนักศึกษาแผนกเทคโนโลยีสารสนเทศ วิทยาลัยเทคนิคเลยพัฒนา (Loei Tech)

## 📋 สิ่งที่ต้องเตรียม (Prerequisites)

ก่อนเริ่มใช้งาน ต้องติดตั้งโปรแกรมเหล่านี้ในเครื่องของคุณ:

1.  **Node.js** (แนะนำเวอร์ชัน 18 หรือสูงกว่า) - [ดาวน์โหลด](https://nodejs.org/)
2.  **MongoDB** (สำหรับฐานข้อมูล) - [ดาวน์โหลด](https://www.mongodb.com/try/download/community) หรือใช้ MongoDB Atlas (Cloud)

## 🛠️ วิธีการติดตั้ง (Installation)

### 1. Clone โปรเจกต์

```bash
git clone <repository-url>
cd it-student-repo
```

### 2. ติดตั้ง Dependencies ฝั่ง Server (Backend)

```bash
cd server
npm install
```

### 3. ติดตั้ง Dependencies ฝั่ง Client (Frontend)

```bash
cd ../client
npm install
```

## ⚙️ การตั้งค่า Environment Variables

### ฝั่ง Server (`server/.env`)

สร้างไฟล์ `.env` ในโฟลเดอร์ `server` และกำหนดค่าดังนี้:

```env
# ตั้งค่า Port ของ Server
PORT=8000

# การเชื่อมต่อฐานข้อมูล MongoDB (เปลี่ยนเป็น Connection String ของคุณถ้าใช้ Atlas)
MONGO_URI=mongodb://localhost:27017/it_student_repo

# รหัสลับสำหรับ JWT Token (ตั้งอะไรก็ได้ให้คาดเดายาก)
JWT_SECRET=your_jwt_secret_key_change_this

# Google OAuth Client ID (ใช้ค่านี้สำหรับโปรเจกต์นี้)
GOOGLE_CLIENT_ID=616609232183-qth5o01fmleiketoq9o4atjavcuom9bt.apps.googleusercontent.com

# (Optional) ถ้าสร้าง Client Secret ใหม่มาใส่ตรงนี้
# GOOGLE_CLIENT_SECRET=
```

### ฝั่ง Client (`client/.env`)

สร้างไฟล์ `.env` ในโฟลเดอร์ `client`:

```env
VITE_API_URL=http://localhost:8000/api
```

## 🚀 วิธีการรันโปรเจกต์ (Running the App)

ต้องรัน Client และ Server พร้อมกันใน 2 Terminal

### Terminal 1: รัน Server (Backend)

```bash
cd server
npm run dev
```
*Server จะเริ่มทำงานที่ http://localhost:8000*

### Terminal 2: รัน Client (Frontend)

```bash
cd client
npm run dev
```
*Web App จะเปิดได้ที่ http://localhost:3000*

## 🔑 ระบบสมาชิกระดับวิทยาลัย (College Member)

ระบบจะตรวจสอบประเภทผู้ใช้จากอีเมลที่ใช้ล็อกอินอัตโนมัติ:

- **College Member (คนในวิทยาลัย)**: ต้องใช้อีเมลโดเมน `@loeitech.ac.th` และมีรหัสนักศึกษา 11 หลักที่มีรหัสแผนก `31901`
- **General (บุคคลทั่วไป)**: อีเมลอื่นๆ (เช่น @gmail.com) หรืออีเมลวิทยาลัยที่ไม่ใช่รูปแบบนักศึกษาแผนกไอที

## 📝 เทคโนโลยีที่ใช้ (Tech Stack)

- **Frontend**: React, Vite, TailwindCSS, TypeScript
- **Backend**: Node.js, Express, MongoDB, Mongoose
- **Authentication**: JWT, Google OAuth 2.0
